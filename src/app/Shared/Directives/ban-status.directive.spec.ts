import { BanStatusDirective } from './ban-status.directive';

describe('BanStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new BanStatusDirective();
    expect(directive).toBeTruthy();
  });
});
